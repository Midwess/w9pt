//! `SeaORM` Entity, @generated by sea-orm-codegen 1.1.20

use sea_orm::entity::prelude::*;

#[derive(Copy, Clone, Default, Debug, DeriveEntity)]
pub struct Entity;

impl EntityName for Entity {
    fn schema_name(&self) -> Option<&str> {
        Some("public")
    }
    fn table_name(&self) -> &str {
        "w9pt_fs_state_mutation_results"
    }
}

#[derive(Clone, Debug, PartialEq, DeriveModel, DeriveActiveModel, Eq)]
pub struct Model {
    pub filesystem_id: Vec<u8>,
    pub mutation_id: Vec<u8>,
    pub request_fingerprint: Vec<u8>,
    pub client_incarnation_id: Vec<u8>,
    pub writer_scope_id: Vec<u8>,
    pub writer_incarnation_id: Vec<u8>,
    pub fencing_token: Decimal,
    pub result_kind: i32,
    pub result_format: i32,
    pub result_bytes: Vec<u8>,
    pub committed_revision: Decimal,
    pub retention_horizon: Decimal,
    pub record_revision: Decimal,
}

#[derive(Copy, Clone, Debug, EnumIter, DeriveColumn)]
pub enum Column {
    FilesystemId,
    MutationId,
    RequestFingerprint,
    ClientIncarnationId,
    WriterScopeId,
    WriterIncarnationId,
    FencingToken,
    ResultKind,
    ResultFormat,
    ResultBytes,
    CommittedRevision,
    RetentionHorizon,
    RecordRevision,
}

#[derive(Copy, Clone, Debug, EnumIter, DerivePrimaryKey)]
pub enum PrimaryKey {
    FilesystemId,
    MutationId,
}

impl PrimaryKeyTrait for PrimaryKey {
    type ValueType = (Vec<u8>, Vec<u8>);
    fn auto_increment() -> bool {
        false
    }
}

#[derive(Copy, Clone, Debug, EnumIter)]
pub enum Relation {}

impl ColumnTrait for Column {
    type EntityName = Entity;
    fn def(&self) -> ColumnDef {
        match self {
            Self::FilesystemId => ColumnType::VarBinary(StringLen::None).def(),
            Self::MutationId => ColumnType::VarBinary(StringLen::None).def(),
            Self::RequestFingerprint => ColumnType::VarBinary(StringLen::None).def(),
            Self::ClientIncarnationId => ColumnType::VarBinary(StringLen::None).def(),
            Self::WriterScopeId => ColumnType::VarBinary(StringLen::None).def(),
            Self::WriterIncarnationId => ColumnType::VarBinary(StringLen::None).def(),
            Self::FencingToken => ColumnType::Decimal(Some((20u32, 0u32))).def(),
            Self::ResultKind => ColumnType::Integer.def(),
            Self::ResultFormat => ColumnType::Integer.def(),
            Self::ResultBytes => ColumnType::VarBinary(StringLen::None).def(),
            Self::CommittedRevision => ColumnType::Decimal(Some((20u32, 0u32))).def(),
            Self::RetentionHorizon => ColumnType::Decimal(Some((20u32, 0u32))).def(),
            Self::RecordRevision => ColumnType::Decimal(Some((20u32, 0u32))).def(),
        }
    }
}

impl RelationTrait for Relation {
    fn def(&self) -> RelationDef {
        panic!("No RelationDef")
    }
}

impl ActiveModelBehavior for ActiveModel {}
